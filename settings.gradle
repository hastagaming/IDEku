pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Gunakan jalur absolut yang dipahami GitHub Runner
        flatDir {
            dirs 'app/libs'
        }
    }
}
rootProject.name = "IDEku"
include ':app'
