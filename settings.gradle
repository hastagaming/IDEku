pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    // Ubah ke PREFER_SETTINGS agar lebih fleksibel
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "IDEku"
include ':app'
include ':terminal-view-lib' // Tambahkan ini
